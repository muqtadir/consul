apiVersion: v1
data:
  connection.json: |
    {
      "retry_join": ["consul-0.consul.monitoring.svc"]
    }
kind: ConfigMap
metadata:
  labels:
    app: consul
    app.kubernetes.io/instance: consul
    chart: consul-7.0.12
    heritage: Tiller
    release: consul
  name: consul-configuration
  namespace: monitoring
