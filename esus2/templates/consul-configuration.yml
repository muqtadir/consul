apiVersion: v1
data:
  config.json: |
    {
    "datacenter":"devqa"
    }
kind: ConfigMap
metadata:
  labels:
    app: consul
    app.kubernetes.io/instance: consul
    chart: consul-7.0.15
    heritage: Tiller
    release: consul
  name: consul-configuration
  namespace: monitoring
